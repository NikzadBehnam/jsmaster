// Naming Conventions: Variables and function names should be meaningful and use camelCase.
let userName = "Alice"; // camelCase
function calculateTotalPrice() {
  // Function logic
}

// Indentation: Consistent indentation improves code readability. Typically, 2 or 4 spaces per indentation level are used.
function checkEligibility(age) {
  if (age >= 18) {
    return "Eligible";
  } else {
    return "Not Eligible";
  }
}

// Semicolons: Although semicolons at the end of statements are optional in JavaScript, it’s recommended to use them to avoid potential issues.

let x = 10;
let y = 20;
let sum = x + y;
