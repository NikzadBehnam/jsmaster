var a = 20;

if (a === 20) {
  // here if we use let, the variable out of IF block is undefined, if we use var it is 10
  var b = 10;
}

console.log(b);

/* 
استفاده از لت و کانست بعد از ای اس 6 معمولتر میباشد
*/
